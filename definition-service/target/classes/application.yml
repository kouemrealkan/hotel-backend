spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hotel_definition_service
    username: root
    password: emreSskla25
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
server:
  port: 8080